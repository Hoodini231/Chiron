# Copy to .env, add your key privately, then restart npm start.
# Set one or both keys. Gemini is preferred when both are present.
# Override with LLM_PROVIDER=openai or LLM_PROVIDER=gemini.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
